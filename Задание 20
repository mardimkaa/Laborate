#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    int N;
    printf("Введите номер задания:");
    scanf("%d", &N);
    if (N == 1) {
        int n;
     
        printf("Введите N: ");
        scanf("%i",&n);
     
        int a[n];
     
        int i;
        printf("Введите массив а\n");
        for (i=0; i<n; ++i){
            printf("a[%i] : ",i);
            scanf("%i",&a[i]);
        }
        int k=0;
        for (i=1; i<n; ++i){
                if (a[k]!=a[i]){
                    ++k;
                    a[k]=a[i];
               }
        }
        ++k;
    printf("Вывод");
    for (i=0; i<k; ++i)
    printf("%i: %i\n",i+1,a[i]);
 
    return 0;
        
        printf("Вывод массива a\n");
        for (i = 0; i<n; i++)
          printf("%d\n", a[i]);
        
        
    }
       if (N == 2){
            int n;

        printf("N: ");
        scanf("%i",&n);
        int a[n];

        int i;
        for (i=0; i<n; ++i){
            printf("a[%i] : ",i+1);
            scanf("%i",&a[i]);
        }
        int num, temp, k;
        while(i < n){
            num = 0;
            for(int i2 = 1; i2 < n; i++){
               if(a[i] == a[i2]){
                   num++;
               }
               if (num != 2){
                   i++;
               }
               else{
                for(i = 0; i < n - 1; i++){
                    if(a[i] = a[i +1]){
                        a[i] = 0;
                        a[i + 1] = 0;
                        break;
                    }
                    
                } 
               }
            }
            
        }
                    int c[n];
            for (i = 0; i < n; i++){
                if (a[i] == 0){
                    continue;
                }
                else{
                c[i] = a[i];   
                }
            }
        printf("Размер массива a %d \n", n-2);
            for (i = 0; i<n; i++)
            printf("%d\n", a[i]);
      
    }

    if (N == 3){
        int n;
 
    printf("N: ");
    scanf("%i",&n);
    int a[n];
 
    int i;
    for (i=0; i<n; ++i){
        printf("a[%i] : ",i);
        scanf("%i",&a[i]);
    }
 
    int c = 0;
 
    for (i=0; i<n; i++) {
        if (a[i] % 2 != 0){
           c = a[i];
        }
    }
    
    if (c != 0){
        for (i=0; i<n; i++) {
            if (a[i] % 2 != 0){
               printf("%d\n", a[i]+c);
            }
            else{
                printf("%d\n", a[i]);
            }
            
        }
    }
        
 
 
    return 0;
    }

    if (N == 4){
        int n;

        printf("N: ");
        scanf("%i",&n);
        int a[n];

        int i;
        for (i=0; i<n; ++i){
            printf("a[%i] : ",i);
            scanf("%i",&a[i]);
        }
